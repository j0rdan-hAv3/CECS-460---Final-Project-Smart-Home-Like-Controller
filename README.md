this project idea is to bind two esp (wire or wifi) in order to transmit events from the server (mouse click for example) and from the other esp which handle the device (physical buttons for example)

thus, we can update the web app with real time state of the device (in this case, 1 RGB LED) or trigger them by changing there states on the screen

languages: French and English
